// This component is no longer used - detail views are inline in dashboards
// Kept as a minimal export to avoid import errors
const AssignmentDetail = ({ assignmentId, onBack }: { assignmentId: string; onBack: () => void }) => {
  return null;
};

export default AssignmentDetail;
